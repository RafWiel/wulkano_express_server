// const {CarService} = require('../models');
// const {CarTire} = require('../models');
// const {Client} = require('../models');
// const {Company} = require('../models');
// const {Deposit} = require('../models');
// const {DepositTire} = require('../models');
// const {Directory} = require('../models');
// const {Mechanic} = require('../models');
// const {TruckService} = require('../models');
// const {TruckTire} = require('../models');

//Jesli jest blad to wyremuj na chwile. Musi istniec tabela
// CarService.belongsTo(Client, { as: 'Client', foreignKey: 'clientId'});
// CarService.belongsTo(Directory, { as: 'Directory', foreignKey: 'directoryId'});
// CarTire.belongsTo(CarService, { as: 'CarTire', foreignKey: 'serviceId'});
// Deposit.belongsTo(Client, { as: 'Client', foreignKey: 'clientId'});
// Deposit.belongsTo(Directory, { as: 'Directory', foreignKey: 'directoryId'});
// DepositTire.belongsTo(Deposit, { as: 'Tire', foreignKey: 'depositId'});
// Mechanic.belongsTo(TruckService, { as: 'TruckService', foreignKey: 'serviceId'});
// TruckService.belongsTo(Company, { as: 'Company', foreignKey: 'companyId'});
// TruckService.belongsTo(Directory, { as: 'Directory', foreignKey: 'directoryId'});
// TruckTire.belongsTo(TruckService, { as: 'TruckService', foreignKey: 'serviceId'});

